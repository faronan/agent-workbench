{
  "name": "cc-fork-matrix",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "cc-fork-matrix": "./dist/cli.js"
  },
  "scripts": {
    "build": "node scripts/build.mjs",
    "check": "pnpm test && pnpm typecheck && pnpm lint && pnpm build",
    "lint": "biome check .",
    "test": "node --experimental-strip-types --test \"test/**/*.test.ts\"",
    "typecheck": "node --experimental-strip-types scripts/typecheck.mjs"
  },
  "engines": {
    "node": ">=24.0.0",
    "pnpm": ">=11.0.0"
  },
  "devDependencies": {
    "esbuild": "^0.28.0"
  }
}
